---allure---
# allure --version
#npm install -g allure-commandline
#npm install -D allure-playwright
#allure serve
#contrl+/ -- to comment the line
# control+space bar for checking the fixures used in async fn. async({control+space})
#view--reload window >> when u add any extention